@inject NavigationManager NavigationManager

@code {
    protected override void OnInitialized()
    {
        NavigationManager.NavigateTo($"/authenticate/login?returnUrl={Uri.EscapeDataString(NavigationManager.Uri)}", true);
    }
}